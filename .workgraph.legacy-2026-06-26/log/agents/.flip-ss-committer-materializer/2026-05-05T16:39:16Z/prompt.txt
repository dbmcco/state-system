You are working in: /Users/braydon/projects/experiments/state-system

Task: .flip-ss-committer-materializer - FLIP: ss-committer-materializer

Description:
```wg-contract
schema = 1
mode = "core"
objective = "FLIP: ss-committer-materializer"
non_goals = [
  "No fallbacks/retries/guardrails unless acceptance requires it",
]
touch = [
]
acceptance = [
]
max_files = 25
max_loc = 800
pit_stop_after = 3
auto_followups = true
```

Run FLIP (Fidelity via Latent Intent Probing) evaluation for task 'ss-committer-materializer'.

Context from dependencies:
From ss-committer-materializer logs: 2026-04-29T19:00:18.005184+00:00 Spawned by wg spawn --executor claude --model opus
From ss-committer-materializer logs: 2026-04-29T19:21:16.801863+00:00 Spawned by wg spawn --executor claude --model opus
From ss-committer-materializer logs: 2026-04-29T20:01:47.285192+00:00 Spawned by wg spawn --executor claude --model opus
From ss-committer-materializer logs: 2026-04-29T21:32:53Z Validated manually: committer/materializer handles accepted state and memory proposals, pending approval actions, rejected unresolved evidence, protected snapshot patches, no-op receipts, duplicate model output replay, derived snapshots, queued rollups, and review signals; 29 unittest checks, compileall, diff check, and coredrift green.
From ss-committer-materializer logs: 2026-04-29T21:32:53Z Task marked as done

## Coredrift Protocol
- Treat the `wg-contract` block (in the task description) as binding.
- At start and just before completion, run:
  ./.workgraph/drifts check --task .flip-ss-committer-materializer --write-log --create-followups
- If you need to change scope, update touch globs:
  ./.workgraph/coredrift contract set-touch --task .flip-ss-committer-materializer <globs...>
- If `hardening_in_core` is flagged, avoid adding guardrails in the core task; do/complete the `harden:` follow-up task instead.

## Superpowers Protocol
- If Superpowers-style skills are available in this environment, use:
  /brainstorming before code
  /test-driven-development for behavior changes
  /verification-before-completion before `wg done`
- If not available, follow the same phases explicitly.

## Model-Mediated Protocol
- Separate pipes vs decisions (facts/execution vs judgment).
- If a Model-Mediated Architecture skill is available, apply it (model decides; code executes).
- Log key decisions/deviations in `wg log`, and prefer follow-up tasks over bloating the current task.

## archdrift Protocol
- If this task includes an `archdrift` block (in the description), run:
  ./.workgraph/archdrift wg check --task .flip-ss-committer-materializer --write-log --create-followups
- Or run the unified check (auto/all strategy can include archdrift):
  ./.workgraph/drifts check --task .flip-ss-committer-materializer --write-log --create-followups
- Artifacts live under `.workgraph/.archdrift/`.


## uxdrift Protocol
- If this task includes a `uxdrift` block (in the description), run:
  ./.workgraph/uxdrift wg check --task .flip-ss-committer-materializer --write-log --create-followups
- Or run the unified check (runs uxdrift when a spec is present):
  ./.workgraph/drifts check --task .flip-ss-committer-materializer --write-log --create-followups
- If it fails due to missing URL, set `url = "..."` in the `uxdrift` block or pass --url.


## therapydrift Protocol
- If this task includes a `therapydrift` block (in the description), run:
  ./.workgraph/therapydrift wg check --task .flip-ss-committer-materializer --write-log --create-followups
- Or run the unified check (runs therapydrift when a spec is present):
  ./.workgraph/drifts check --task .flip-ss-committer-materializer --write-log --create-followups
- Artifacts live under `.workgraph/.therapydrift/`.


## fixdrift Protocol
- If this task includes a `fixdrift` block (in the description), run:
  ./.workgraph/fixdrift wg check --task .flip-ss-committer-materializer --write-log --create-followups
- Or run the unified check (runs fixdrift when a spec is present):
  ./.workgraph/drifts check --task .flip-ss-committer-materializer --write-log --create-followups
- Artifacts live under `.workgraph/.fixdrift/`.


## yagnidrift Protocol
- If this task includes a `yagnidrift` block (in the description), run:
  ./.workgraph/yagnidrift wg check --task .flip-ss-committer-materializer --write-log --create-followups
- Or run the unified check (runs yagnidrift when a spec is present):
  ./.workgraph/drifts check --task .flip-ss-committer-materializer --write-log --create-followups
- Artifacts live under `.workgraph/.yagnidrift/`.


## redrift Protocol
- If this task includes a `redrift` block (in the description), run:
  ./.workgraph/redrift wg check --task .flip-ss-committer-materializer --write-log --create-followups
- Or run the unified check (runs redrift when a spec is present):
  ./.workgraph/drifts check --task .flip-ss-committer-materializer --write-log --create-followups
- Artifacts live under `.workgraph/.redrift/`.


## Workgraph Rules
- Stay focused on this task.
- Log progress: wg log .flip-ss-committer-materializer "message"
- When complete: wg done .flip-ss-committer-materializer
- If blocked: wg fail .flip-ss-committer-materializer --reason "description"
