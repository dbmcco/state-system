You are working in: /Users/braydon/projects/experiments/state-system

Task: state-evaluator-infra-repair-v0 - Repair Workgraph evaluator LLM configuration

Description:
```wg-contract
schema = 1
mode = "core"
objective = "Repair local Workgraph evaluator configuration so FLIP and direct evaluator gates can run without coordinator manual override."
non_goals = [
  "Do not change product schemas or package behavior",
  "Do not require private API keys to be committed"
]
touch = [
  ".workgraph/config.toml",
  "docs/reports/*evaluator*",
  ".workgraph/graph.jsonl"
]
acceptance = [
  "Document whether evaluator should use native Anthropic API config or Claude CLI",
  "At least one FLIP gate and one direct eval complete successfully on a small completed task",
  "If credentials are unavailable, record exact required local setup and leave task blocked with no product impact"
]
max_files = 4
max_loc = 300
pit_stop_after = 3
auto_followups = true
```

Created by state-operational-eval-gate-v0. Current failure: no native Anthropic API key and Claude CLI fallback exits with code 1 for FLIP/direct evaluator calls.

Context from dependencies:
From state-operational-eval-gate-v0 logs: 2026-05-18T19:09:22.959638+00:00 Coredrift: yellow (churn_files, churn_loc, hardening_in_core, scope_drift) | next: Revert out-of-scope file changes or expand touch globs
From state-operational-eval-gate-v0 logs: 2026-05-18T19:09:26.945173+00:00 Coordinator takeover after worker exit. Start drift yellow due inherited repo-wide package-layer churn; this task will only write docs/reports/2026-05-18-operational-model-eval-gate.md and Workgraph logs/follow-ups.
From state-operational-eval-gate-v0 logs: 2026-05-18T19:11:30.920417+00:00 Coredrift: yellow (churn_files, churn_loc, hardening_in_core, scope_drift) | next: Revert out-of-scope file changes or expand touch globs
From state-operational-eval-gate-v0 logs: 2026-05-18T19:11:35.415342+00:00 Eval gate complete by coordinator takeover. Attempted FLIP for 10 relevant gates; all failed at Claude CLI evaluator call. Direct eval smoke for source-module and personal state also failed due missing native Anthropic key and Claude CLI fallback exit 1. Wrote docs/reports/2026-05-18-operational-model-eval-gate.md; created state-evaluator-infra-repair-v0. Validation green: 127 examples, focused 33 tests, git diff --check clean. End drift yellow only inherited repo churn.
From state-operational-eval-gate-v0 logs: 2026-05-18T19:11:38.435972+00:00 Task marked as done

## Coredrift Protocol
- Treat the `wg-contract` block (in the task description) as binding.
- At start and just before completion, run:
  ./.workgraph/drifts check --task state-evaluator-infra-repair-v0 --write-log --create-followups
- If you need to change scope, update touch globs:
  ./.workgraph/coredrift contract set-touch --task state-evaluator-infra-repair-v0 <globs...>
- If `hardening_in_core` is flagged, avoid adding guardrails in the core task; do/complete the `harden:` follow-up task instead.

## Superpowers Protocol
- If Superpowers-style skills are available in this environment, use:
  /brainstorming before code
  /test-driven-development for behavior changes
  /verification-before-completion before `wg done`
- If not available, follow the same phases explicitly.

## Model-Mediated Protocol
- Separate pipes vs decisions (facts/execution vs judgment).
- If a Model-Mediated Architecture skill is available, apply it (model decides; code executes).
- Log key decisions/deviations in `wg log`, and prefer follow-up tasks over bloating the current task.

## archdrift Protocol
- If this task includes an `archdrift` block (in the description), run:
  ./.workgraph/archdrift wg check --task state-evaluator-infra-repair-v0 --write-log --create-followups
- Or run the unified check (auto/all strategy can include archdrift):
  ./.workgraph/drifts check --task state-evaluator-infra-repair-v0 --write-log --create-followups
- Artifacts live under `.workgraph/.archdrift/`.


## uxdrift Protocol
- If this task includes a `uxdrift` block (in the description), run:
  ./.workgraph/uxdrift wg check --task state-evaluator-infra-repair-v0 --write-log --create-followups
- Or run the unified check (runs uxdrift when a spec is present):
  ./.workgraph/drifts check --task state-evaluator-infra-repair-v0 --write-log --create-followups
- If it fails due to missing URL, set `url = "..."` in the `uxdrift` block or pass --url.


## therapydrift Protocol
- If this task includes a `therapydrift` block (in the description), run:
  ./.workgraph/therapydrift wg check --task state-evaluator-infra-repair-v0 --write-log --create-followups
- Or run the unified check (runs therapydrift when a spec is present):
  ./.workgraph/drifts check --task state-evaluator-infra-repair-v0 --write-log --create-followups
- Artifacts live under `.workgraph/.therapydrift/`.


## fixdrift Protocol
- If this task includes a `fixdrift` block (in the description), run:
  ./.workgraph/fixdrift wg check --task state-evaluator-infra-repair-v0 --write-log --create-followups
- Or run the unified check (runs fixdrift when a spec is present):
  ./.workgraph/drifts check --task state-evaluator-infra-repair-v0 --write-log --create-followups
- Artifacts live under `.workgraph/.fixdrift/`.


## yagnidrift Protocol
- If this task includes a `yagnidrift` block (in the description), run:
  ./.workgraph/yagnidrift wg check --task state-evaluator-infra-repair-v0 --write-log --create-followups
- Or run the unified check (runs yagnidrift when a spec is present):
  ./.workgraph/drifts check --task state-evaluator-infra-repair-v0 --write-log --create-followups
- Artifacts live under `.workgraph/.yagnidrift/`.


## redrift Protocol
- If this task includes a `redrift` block (in the description), run:
  ./.workgraph/redrift wg check --task state-evaluator-infra-repair-v0 --write-log --create-followups
- Or run the unified check (runs redrift when a spec is present):
  ./.workgraph/drifts check --task state-evaluator-infra-repair-v0 --write-log --create-followups
- Artifacts live under `.workgraph/.redrift/`.


## Workgraph Rules
- Stay focused on this task.
- Log progress: wg log state-evaluator-infra-repair-v0 "message"
- When complete: wg done state-evaluator-infra-repair-v0
- If blocked: wg fail state-evaluator-infra-repair-v0 --reason "description"
