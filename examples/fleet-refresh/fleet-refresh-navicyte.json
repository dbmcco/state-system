{
  "id": "fleet_refresh_manifest.navicyte",
  "description": "Public refresh manifest for the Navicyte state instance. Source adapters are delegated to the source owner; this manifest declares the refresh path and records evidence without copying private corpus.",
  "default_ttl_seconds": 3600,
  "instances": [
    {
      "id": "fleet_instance.navicyte",
      "state_root": "/path/to/navicyte/state-system",
      "instance_ref": "state_instance.navicyte",
      "agent_ref": "agent.navicyte",
      "persona_ref": "persona.navicyte",
      "package_id": "instance_agent_package.navicyte",
      "review_goal": "Refresh Navicyte package evidence and surface any source gaps before answering venture questions.",
      "preflight_mode": "export_only",
      "output_dirs": {
        "instance_preflight": "instance-preflight",
        "instance_source_freshness": "instance-source-freshness",
        "instance_understanding": "instance-understanding",
        "instance_agent_package": "instance-agent-package"
      },
      "adapter_commands": [
        {
          "id": "adapter.navicyte.notion.refresh",
          "description": "Delegated Notion workspace refresh. Replace with the source-owner adapter in deployed environments.",
          "cwd": "/path/to/navicyte",
          "argv": ["echo", "notion refresh delegated to source owner"],
          "required": false,
          "timeout_seconds": 300
        },
        {
          "id": "adapter.navicyte.email.refresh",
          "description": "Delegated email/mailbox refresh. Replace with the source-owner adapter in deployed environments.",
          "cwd": "/path/to/navicyte",
          "argv": ["echo", "email refresh delegated to source owner"],
          "required": false,
          "timeout_seconds": 300
        }
      ]
    }
  ],
  "entity_current_state": {
    "roots": [
      {
        "state_root": "/path/to/navicyte/state-system",
        "label": "navicyte"
      }
    ],
    "output_dir": "entity-current-state"
  }
}
